function multiplyTable(n) {

    let currentNum = n;

    for (let i = 1; i <= 10; i++) {
        
        
        console.log(`${currentNum} X ${i} = ${currentNum * i}`);
        
    }


}
multiplyTable(5)